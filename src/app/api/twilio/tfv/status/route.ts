import {NextResponse} from "next/server";import {createOperationServices} from "@/application/operations/create-operation-services";import {createClient} from "@/lib/supabase/server";import {supabaseConfigurationErrorResponse} from "@/lib/http-configuration";export const runtime="nodejs";export async function POST(request:Request){const configurationError=supabaseConfigurationErrorResponse();if(configurationError)return configurationError;const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});try{const body=await request.json().catch(()=>({})) as {limit?:number;applicationId?:string};const services=await createOperationServices();const results=await services.statusSync.execute({limit:body.limit,applicationId:body.applicationId,actorId:user.id});return NextResponse.json({results});}catch{return NextResponse.json({error:"Unable to synchronize TFV status."},{status:502});}}

